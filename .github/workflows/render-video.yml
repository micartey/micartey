name: Render video

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  render:
    name: Render video
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - uses: actions/setup-node@main
      - run: npm install
      - run: npm run build
